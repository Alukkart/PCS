<Window x:Class="PCS.Windows.AuthorManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление авторами"
        Width="930"
        Height="560"
        MinWidth="850"
        MinHeight="520"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" Margin="0,0,0,10">
            <Button Width="90" Margin="0,0,8,0" Click="CreateNewButton_OnClick" Content="Новый" />
            <Button Width="120" Margin="0,0,8,0" Click="SaveButton_OnClick" Content="Сохранить" />
            <Button Width="90" Margin="0,0,8,0" Click="DeleteButton_OnClick" Content="Удалить" />
            <Button Width="90" Click="CloseButton_OnClick" Content="Закрыть" />
        </WrapPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <DataGrid x:Name="AuthorsDataGrid"
                      Margin="0,0,10,0"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      SelectionChanged="AuthorsDataGrid_OnSelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="2*" Binding="{Binding FirstName}" Header="Имя" />
                    <DataGridTextColumn Width="2*" Binding="{Binding LastName}" Header="Фамилия" />
                    <DataGridTextColumn Width="*" Binding="{Binding BirthDate, StringFormat=d}" Header="Дата рождения" />
                    <DataGridTextColumn Width="*" Binding="{Binding Country}" Header="Страна" />
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="1"
                    Padding="10"
                    BorderBrush="#CCCCCC"
                    BorderThickness="1"
                    CornerRadius="6">
                <StackPanel>
                    <TextBlock Margin="0,0,0,4" FontWeight="SemiBold" Text="Имя" />
                    <TextBox x:Name="FirstNameTextBox" Margin="0,0,0,10" />

                    <TextBlock Margin="0,0,0,4" FontWeight="SemiBold" Text="Фамилия" />
                    <TextBox x:Name="LastNameTextBox" Margin="0,0,0,10" />

                    <TextBlock Margin="0,0,0,4" FontWeight="SemiBold" Text="Дата рождения" />
                    <DatePicker x:Name="BirthDatePicker" Margin="0,0,0,10" />

                    <TextBlock Margin="0,0,0,4" FontWeight="SemiBold" Text="Страна" />
                    <TextBox x:Name="CountryTextBox" />
                </StackPanel>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2"
                   Margin="0,10,0,0"
                   Foreground="#666666"
                   Text="Удаление автора удаляет все связанные книги (каскадное удаление)." />
    </Grid>
</Window>
